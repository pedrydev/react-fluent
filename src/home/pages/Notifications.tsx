import TrackView from "@/core/track/TrackView.tsx";

export default function Notifications() {
  return (
    <TrackView target="notification-span">
      <span>Notifications</span>
    </TrackView>
  );
}
